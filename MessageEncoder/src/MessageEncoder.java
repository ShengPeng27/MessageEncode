
/**
 * @author sp29011
 * To encode the message. There are two methods in this program, one is to
 * shift the letters, another is to  alternate the letters in the string
 */
public interface MessageEncoder {
	public String encode(String plainText);
}
